@model FE.Identity.Models.Servicios

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Servicios</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdServicio" />
            <div class="form-group">
                <label class="control-label">Descripcion Servicio</label>
                <input asp-for="DescripcionServicio" class="form-control" />
                <span asp-validation-for="DescripcionServicio" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
